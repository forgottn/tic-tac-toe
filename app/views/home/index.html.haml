- content_for(:head) do
  = javascript_include_tag 'tic-tac-toe.js'

%h1 Tic Tac Toe

#status

%table#board
  %tbody
    - @board.each_slice(3) do |row|
      %tr
        - row.each do |cell|
          %td{:id => "cell_#{cell}"}
